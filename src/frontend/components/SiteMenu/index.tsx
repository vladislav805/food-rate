import * as React from 'react';

type ISiteMenuProps = {};

const SiteMenu: React.FC<ISiteMenuProps> = () => {
    return (
        <div>
            menu
        </div>
    );
};

export default SiteMenu;
